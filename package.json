{
  "name": "church-presenter",
  "private": true,
  "version": "0.0.1",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev -w @cp/desktop",
    "build": "npm run build -w @cp/desktop",
    "lint": "npm run lint --workspaces --if-present",
    "typecheck": "npm run typecheck -w @cp/desktop"
  }
}
